[build]
builder = "nixpacks"

[build.nixpacks]
# Deploying from the repo root: install Python deps via root requirements.txt,
# then start the wrapper that changes into Swarm Agent/backend.
providers = ["python"]

[env]
PYTHONPATH = "/app"

[start]
cmd = "python run.py"
